{
    "name": "json-server",
    "version": "1.0.0",
    "scripts": {
        "start": "json-server --watch db.json --port $PORT",
        "prestart": "npm install",
        "build": "echo 'No build step required'"
    },
    "dependencies": {
        "json-server": "^0.17.4"
    }
}
