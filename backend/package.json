{
    "name": "json-server",
    "version": "1.0.0",
    "scripts": {
           "start": "json-server --watch db.json --port $PORT",
           "build": "echo 'No build step required'"
    },
    "dependencies": {
      "json-server": "^1.0.0-beta.3"
    }
  }
  